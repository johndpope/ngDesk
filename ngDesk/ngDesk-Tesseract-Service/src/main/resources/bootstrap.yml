spring:
  application:
    name: ngdesk-tesseract-service_v1
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: prod
  cloud:
    config:
      uri: http://config-server-service:8888
