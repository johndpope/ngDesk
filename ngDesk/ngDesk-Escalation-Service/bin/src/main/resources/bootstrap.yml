spring:
  application:
    name: ngdesk-escalation-service_v1
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://config-server-service:8888